openapi: 3.0.0
info:
  title: mini-agent Providers Domain OpenSpec
  version: 0.1.15
  description: OpenAPI specification for LLM providers registry and client initialization.
paths:
  /providers/list:
    get:
      summary: List registered providers
      responses:
        '200':
          description: List of supported LLM provider definitions.
